# AgentDesk — Plantilla de configuracion
# Copia este archivo como .env y rellena tus valores reales.
# NUNCA subas el archivo .env a un repositorio.

# API Key de Google Gemini
# Obtén la tuya en: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=TU_API_KEY_AQUI

# Hash bcrypt de la contrasena de acceso
# Genera el hash con:
#   python -c "import bcrypt; print(bcrypt.hashpw(b'TuClave', bcrypt.gensalt(12)).decode())"
MASTER_PASSWORD_HASH=TU_HASH_BCRYPT_AQUI

# Licencia RSA local (ADR-0022) — OPCIONAL. Sin licencia el sistema opera
# en modo desktop libre. Para instalarla: pega el contenido de license.key
# en el SecurityPanel, o copia el archivo a %APPDATA%\AgentDesk\license.key.
# Ruta alternativa al archivo de licencia (dejar sin definir en escritorio):
# AGENTDESK_LICENSE_FILE=

# Notificaciones proactivas de alertas SLO (ADR-0027) - OPCIONALES.
# Sin configurar, las alertas quedan solo en el log critico (AUDITORIA_SEGURIDAD).
# Slack: Incoming Webhook del canal de operaciones.
# AGENTDESK_SLACK_WEBHOOK=https://hooks.slack.com/services/XXX/YYY/ZZZ
# WhatsApp (Cloud API de Meta): token + phone_id del emisor y numero destino
# en formato internacional sin '+'.
# AGENTDESK_WHATSAPP_TOKEN=
# AGENTDESK_WHATSAPP_PHONE_ID=
# AGENTDESK_WHATSAPP_DESTINO=

# Telemetria Modbus real (ADR-0004/0021) - OPCIONAL. Sin definir, el
# ModbusTelemetryAdapter opera en modo simulador (la app nunca depende de
# un PLC para arrancar). Con ModbusPal u otro esclavo escuchando:
# AGENTDESK_MODBUS_HOST=127.0.0.1:5021
